变量
network: 用了model.inception_resnet_v1.py
log_dir, subdir:日志文件
train_set : 数据集
src_path : 工作目录
model_dir : 
pairs: data/pairs. LFW图片配对，包括正例和反例，

lfw_paths : 元组(图片1路径，图片二路径)的list，是LFW中数据的List
actual_issame : True or False的list，表示上面的每一对是正例还是反例


-- Tensorflow 文档：
-- tf.reshape(
    tensor,
    shape,
    name=None
	)
	reshape 里面的-1是表示剩下全部维度的意思，比如一个18个元素的tensor， [2,-1,3]中-1表示3
-- tf.unstack()


--网络部分变量
--learning_rate_placeholder
--batch_size_placeholder 一个batch的大小
--phase_train_placeholder
--image_paths_placeholder 一张图片的路径
--labels——placeholder 一张图片的label
--input_queue = 队列，每个队列元素包含三行数据，每行数据包括一个image_path_placeholder和一个label_placeholder, 这里不是图片数据

--images_and_labels: 真正图片数据列表，每个元素是一个元组（tf解码图片和label）

--image_batch, labels_batch: g根据images_and_labels 分解出的image_batch 和labels_batch

--prelogits inception_resnet_v1网络本身
--embeddings L2 norm 之后
--learning_rate 
--


参数：
seed : 默认666
model_def : 默认
data_dir : 训练数据路径，母路径
pretrained_model : (非必须) 预处理模型
lfw_dir : lfw数据集位置
lfw_pairs : lfw pair 图片配对
image_size : 读入图片的大小（训练图片）
